import 'package:flutter/material.dart';

class FavoritesScrreen extends StatelessWidget {
  const FavoritesScrreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Filmes Favoritos'),
      ),
      body: Container(),
    );
  }
}
